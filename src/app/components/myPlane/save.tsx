import { DataTypes } from "@/types/libraryDataType";

export interface SaveProps {
    saveData: DataTypes[]
}

const Save = ({ saveData }: SaveProps) => {
    
    return (
        <>
        
        </>
    )
}

export default Save;